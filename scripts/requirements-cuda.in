# GPU/CUDA dependencies (optional)
# This file is kept for legacy compatibility
# Use: pip install -e .[thoth] for ML dependencies with CPU torch
# For CUDA support, install manually: pip install torch[cuda]

# Note: All other dependencies are managed in pyproject.toml
# This file can be removed once all build processes use pyproject.toml 